# Never place credentials or confidential policy data in this file.
LLM_PROVIDER=openai
LLM_MODEL=
OPENAI_API_KEY=
# Frontend: mock = illustrative preview; http = live engine via Vite proxy
VITE_DATA_MODE=http
# Leave empty to use the Vite proxy (/v1 → http://127.0.0.1:8000)
VITE_API_BASE_URL=
