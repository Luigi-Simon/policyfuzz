# Gate A2 compatibility disposition — original candidate

Assessment time: 2026-09-06T07:17:12Z. Result: **blocked; no A2 seal created**.

The unchanged Gate A1 candidate cannot be mapped faithfully into the frozen Task 3 public models. The incompatibility is missing independent source fact dimensions and category distinctions, plus a mismatch between a broad source fact and the narrower meaning of a typed public field. This is a domain-capability mismatch, not a JSON spelling, scalar-conversion, or OR-expansion issue. OR expansion cannot reconstruct an absent fact. Reusing a public field with a different meaning would invent a semantic premise. Marking affected content unsupported is honest but does not satisfy the requirement to preserve all retained expected defects and corrected structured semantics.

Original archive, exact source, manifest bytes, and canonical stable-ID hashes matched the existing A1 ledger; all three stable IDs remain unchanged. The original archive and A1 public seal were not modified or resealed. No additional labels, scenarios, rules, expected effects, or corrected semantics were authored.

The existing validator was invoked with the unchanged A1 archive as a compatibility probe and returned exit 2, `BLIND_ARCHIVE_MEMBERS`, without creating output. This is a packaging rejection, not a semantic test of an A2 package. No schema-bound package was fabricated; schema validation has not passed.

The original candidate is disqualified for current schema-bound scoring pending an authorized resolution. Custody-compatible alternatives are: retain the old seal and exclude this candidate; commission a fresh custodian to author a separate in-scope candidate with new, explicitly late-sealed lineage; or defer for independent human remapping and a documented scope decision. No replacement is created by this action.

Provenance limitations remain: synthetic delegated-agent authorship, late sealing, and human review pending. This is not approved gold or evidence of predevelopment blindness. Runtime `session_confirmed`/`gold` encodings would not establish human approval. Shared-host process separation is not cryptographic isolation. The custodian inspected only the authorized public models, canonical helpers, validator, narrow gate instructions, and private custody input; no feature prompts, specialist implementation, evaluator, or provider were used.

Private assessment retained separately:

- Original input bundle SHA256: `4ddd69047e0a078c3682a418ddf36e4b411ba1598a20eeb52d49f79b8af73cc0`.
- Assessment bundle SHA256: `b00c5ea1bb00e4b1c0493572f7bb6428c26a59aa07e792ef7bc95cc683aebe87`.
- Assessment Library ID: `libfile_67c83ce7fe1c8191ab788ee3a6e1a8d2`; save succeeded.

This public record contains no source text, stable defect IDs, scenario content, labels, or corrected semantics.
