# Benchmark sample ownership

Person 4 owns benchmark execution artifacts and deterministic labels for development and corrected-control runs. Person 2 owns the development/corrected source labels, canonical policy IR, confirmed contracts, and corrected semantics. Person 5 alone holds sealed blind labels before reveal and imports blind artifacts only at the defined custody step.
