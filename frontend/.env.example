# Frontend → engine adapter (temporary until Person 1 /api/v1 exists)
# mock = illustrative preview (default, used by tests)
# http = live engine via Vite proxy
VITE_DATA_MODE=http
# Leave empty to use the Vite proxy (/v1 → http://127.0.0.1:8000)
VITE_API_BASE_URL=
