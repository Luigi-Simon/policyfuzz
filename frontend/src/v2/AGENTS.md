# V2 frontend

Simon owns this namespace and its v2 tests, generated types, route/proxy entry,
and build checks under the user-approved v2 split. Consume generated v2 API
types and validated public evidence. Preserve fixture and partial-status labels;
never display backend-only original records. Label actual Metric runs separately
from Sandbox fixtures; do not claim Judge ran before its adapter is integrated.
